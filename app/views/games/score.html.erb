<h2>Result</h2>

<% if @included_in_grid == false %>
  <p>Sorry but <strong><%= params[:word].upcase%></strong> can't be built out of the grid <%= params[:token] %> </p>
<% elsif @valid %>
  <p>Congratulations <strong><%= params[:word].upcase%></strong> is a valid English Word </p>
<% else %>
  <p>Sorry but <strong><%= params[:word].upcase%></strong> is not a valid English Word </p>
<% end %>
<%= link_to 'Play again', new_path %>
